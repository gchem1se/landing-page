import { StaticImage } from "gatsby-plugin-image";
import * as React from "react"
import "./Button.scss"

const Bullet = ({ title, text, icon }) => {
  return <div className="bulletElement">
      <StaticImage>
      <h3>
          <h4></h4>
      </h3>
  </div>
}

export default Bullet;